# Project 4: Python Data Types

This project contains a Python script that demonstrates various data types in Python, including strings, numbers (integers, floats, complex), and booleans. It also shows the use of some built-in functions and string methods.

## Files
- `data_types.py`: The Python script.

To run the script, use the following command:
```bash
python project04/data_types.py
```
