# Project 9: Functions in Python

This project contains a Python script that demonstrates how to define and use functions in Python. It covers basic functions, functions with default parameters, and functions that accept a variable number of arguments (`*args` and `**kwargs`).

## Files
- `functions.py`: The Python script.

To run the script, use the following command:
```bash
python project09/functions.py
```
